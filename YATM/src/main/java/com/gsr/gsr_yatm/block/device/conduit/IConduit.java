package com.gsr.gsr_yatm.block.device.conduit;

// interface for sharing things across neighboring blocks
public interface IConduit
{
	
	// update on remove, disconnecting if needed
	
	// IConduit is the shared thing between blocks
	
	// IConduit defines creating and maintaining a by touch shared network
	
	// for specific implementations
	// be or have a capability provider to share across all blocks
	
	// increase capacity by each one, each block writes it's capacity fraction individually
		
} // end interface